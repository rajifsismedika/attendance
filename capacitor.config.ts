import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sismedika.attendance',
  appName: 'Attendance',
  webDir: 'public/build'
};

export default config;
